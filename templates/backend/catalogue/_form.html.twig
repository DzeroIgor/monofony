{#{% from 'backend/macros/translatiosForm.html.twig' import translationForm %}#}

<div class="ui two column stackable grid">
    {{ form_errors(form) }}
    <div class="column">
        <div class="ui segment">
{#            <h4 class="ui dividing header">{{ 'app.ui.book_details'|trans }}</h4>#}

            {{ form_row(form.title) }}
            {{ form_row(form.url) }}
            {{ form_row(form.cover) }}
        </div>

    </div>
</div>
